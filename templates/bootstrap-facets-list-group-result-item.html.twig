{#
/**
 * @file
 * Bootstrap list group theme implementation of a facet result item.
 *
 * Available variables:
 * - value: The item value.
 * - show_count: If this facet provides count.
 * - count: The amount of results.
 * - is_active: The item is active.
 *
 * @ingroup themeable
 */
#}
{% if is_active %}
  <span class="glyphicon glyphicon-remove facet-item__status js-facet-deactivate" aria-hidden="true"></span>
{% endif %}
<span class="facet-item__value">{{ value }}</span><span class="glyphicon glyphicon-refresh glyphicon-spin" aria-hidden="true"></span>
{% if show_count %}
  <span class="facet-item__count badge">{{ count }}</span>
{% endif %}
