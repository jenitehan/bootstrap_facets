{#
/**
 * @file
 * Default theme implementation of a facet result item.
 *
 * Available variables:
 * - value: The item value.
 * - show_count: If this facet provides count.
 * - count: The amount of results.
 * - is_active: The item is active.
 *
 * @ingroup themeable
 */
#}
{% if show_count %}
  <span class="facet-item__count badge pull-right">{{ count }}</span>
{% endif %}
{% if is_active %}
  <span class="glyphicon glyphicon-remove facet-item__status js-facet-deactivate" aria-hidden="true"></span>
{% endif %}
<span class="facet-item__value">{{ value }}</span>